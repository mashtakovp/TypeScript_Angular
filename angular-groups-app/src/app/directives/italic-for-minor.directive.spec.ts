import { ItalicForMinorDirective } from './italic-for-minor.directive';

describe('ItalicForMinorDirective', () => {
  it('should create an instance', () => {
    const directive = new ItalicForMinorDirective();
    expect(directive).toBeTruthy();
  });
});
